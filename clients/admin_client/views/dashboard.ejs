<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Panel Admin UDP</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .sidebar {
            min-height: 100vh;
            background-color: #f8f9fa;
            border-right: 1px solid #dee2e6;
        }
        .nav-link {
            color: #495057;
        }
        .nav-link:hover {
            background-color: #e9ecef;
        }
        .nav-link.active {
            background-color: #dc3545 !important;
            color: white !important;
        }
        .stats-card {
            border-left: 4px solid #dc3545;
        }
        .btn-primary, .btn-danger {
            background-color: #dc3545;
            border-color: #dc3545;
        }
        .btn-primary:hover, .btn-danger:hover {
            background-color: #c82333;
            border-color: #bd2130;
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <nav class="col-md-3 col-lg-2 d-md-block sidebar collapse">
                <div class="position-sticky pt-3">
                    <div class="text-center mb-4">
                        <h4 class="text-danger">
                            <i class="fas fa-shield-alt"></i> Admin UDP
                        </h4>
                    </div>
                    
                    <ul class="nav flex-column">
                        <li class="nav-item">
                            <a class="nav-link active" href="/dashboard">
                                <i class="fas fa-tachometer-alt"></i> Dashboard
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/reservas">
                                <i class="fas fa-calendar-check"></i> Reservas
                            </a>
                        </li>
                    </ul>
                    
                    <hr>
                    
                    <div class="text-center">
                        <div class="dropdown">
                            <button class="btn btn-outline-secondary btn-sm dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                <i class="fas fa-user-shield"></i> <%= user.nombre %>
                            </button>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="/logout">
                                    <i class="fas fa-sign-out-alt"></i> Cerrar Sesión
                                </a></li>
                            </ul>
                        </div>
                    </div>
                </div>
            </nav>

            <!-- Main content -->
            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
                <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                    <h1 class="h2"><%= title %></h1>
                </div>

                <!-- Estadísticas -->
                <div class="row mb-4">
                    <div class="col-xl-3 col-md-6 mb-4">
                        <div class="card stats-card h-100">
                            <div class="card-body">
                                <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">
                                    Total Usuarios
                                </div>
                                <div class="h5 mb-0 font-weight-bold">
                                    <%= stats.totalUsers %>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="col-xl-3 col-md-6 mb-4">
                        <div class="card stats-card h-100">
                            <div class="card-body">
                                <div class="text-xs font-weight-bold text-success text-uppercase mb-1">
                                    Total Espacios
                                </div>
                                <div class="h5 mb-0 font-weight-bold">
                                    <%= stats.totalSpaces %>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="col-xl-3 col-md-6 mb-4">
                        <div class="card stats-card h-100">
                            <div class="card-body">
                                <div class="text-xs font-weight-bold text-info text-uppercase mb-1">
                                    Total Reservas
                                </div>
                                <div class="h5 mb-0 font-weight-bold">
                                    <%= stats.totalBookings %>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="col-xl-3 col-md-6 mb-4">
                        <div class="card stats-card h-100">
                            <div class="card-body">
                                <div class="text-xs font-weight-bold text-warning text-uppercase mb-1">
                                    Incidencias Activas
                                </div>
                                <div class="h5 mb-0 font-weight-bold">
                                    <%= stats.totalIncidents %>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Reservas Pendientes -->
                <div class="row">
                    <div class="col-lg-8">
                        <div class="card mb-4">
                            <div class="card-header d-flex justify-content-between align-items-center">
                                <h6 class="m-0 font-weight-bold text-primary">
                                    <i class="fas fa-calendar-check"></i> Reservas Pendientes de Aprobación
                                </h6>
                                <a href="/reservas" class="btn btn-primary btn-sm">
                                    <i class="fas fa-eye"></i> Ver Todas
                                </a>
                            </div>
                            <div class="card-body">
                                <% if (recentBookings && recentBookings.length > 0) { %>
                                    <div class="table-responsive">
                        <table class="table table-bordered">
                                            <thead>
                                                <tr>
                                                    <th>Usuario</th>
                                                    <th>Espacio</th>
                                                    <th>Fecha</th>
                                                    <th>Estado</th>
                                                    <th>Acciones</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <% recentBookings.forEach(function(booking) { %>
                                                    <tr>
                                                        <td><%= booking.usuario_nombre %></td>
                                                        <td><%= booking.espacio_nombre %></td>
                                                        <td><%= new Date(booking.fecha_inicio).toLocaleDateString('es-CL') %></td>
                                                        <td>
                                                            <%
                                                                var statusClasses = {
                                                                    'pendiente': 'warning',
                                                                    'aprobada': 'success',
                                                                    'rechazada': 'danger',
                                                                    'cancelada': 'secondary'
                                                                };
                                                            %>
                                                            <span class="badge bg-<%= statusClasses[booking.estado] || 'secondary' %>">
                                                                <%= booking.estado.charAt(0).toUpperCase() + booking.estado.slice(1) %>
                                                            </span>
                                                        </td>
                                                        <td>
                                                            <% if (booking.estado === 'pendiente') { %>
                                                                <form method="POST" action="/reservas/aprobar" style="display: inline;">
                                                                    <input type="hidden" name="booking_id" value="<%= booking.id %>">
                                                                    <input type="hidden" name="estado" value="aprobada">
                                                                    <button type="submit" class="btn btn-success btn-sm">
                                                                        <i class="fas fa-check"></i> Aprobar
                                                                    </button>
                                                                </form>
                                                                <form method="POST" action="/reservas/aprobar" style="display: inline;">
                                                                    <input type="hidden" name="booking_id" value="<%= booking.id %>">
                                                                    <input type="hidden" name="estado" value="rechazada">
                                                                    <button type="submit" class="btn btn-danger btn-sm">
                                                                        <i class="fas fa-times"></i> Rechazar
                                                                    </button>
                                                                </form>
                                                            <% } else { %>
                                                                <span class="text-muted">-</span>
                                                            <% } %>
                                                        </td>
                                                    </tr>
                                                <% }); %>
                                            </tbody>
                                        </table>
                                    </div>
                                <% } else { %>
                                    <p class="text-muted">No hay reservas pendientes</p>
                                <% } %>
                            </div>
                        </div>
                    </div>

                    <!-- Incidencias Recientes -->
                    <div class="col-lg-4">
                        <div class="card mb-4">
                            <div class="card-header d-flex justify-content-between align-items-center">
                                <h6 class="m-0 font-weight-bold text-warning">
                                    <i class="fas fa-exclamation-triangle"></i> Incidencias Recientes
                                </h6>
                                <a href="/incidencias" class="btn btn-warning btn-sm">
                                    <i class="fas fa-eye"></i> Ver Todas
                                </a>
                            </div>
                            <div class="card-body">
                                <% if (recentIncidents && recentIncidents.length > 0) { %>
                                    <% recentIncidents.forEach(function(incident) { %>
                                        <div class="mb-3 p-2 border rounded">
                                            <div class="d-flex justify-content-between align-items-start">
                                                <div>
                                                    <h6 class="mb-1"><%= incident.espacio_nombre %></h6>
                                                    <p class="mb-1 text-muted small"><%= incident.tipo_incidencia %></p>
                                                    <p class="mb-1 small"><%= incident.descripcion ? incident.descripcion.substring(0, 50) + '...' : 'Sin descripción' %></p>
                                                </div>
                                                <span class="badge bg-<%= incident.estado === 'abierta' ? 'danger' : 'warning' %>">
                                                    <%= incident.estado %>
                                                </span>
                                            </div>
                                        </div>
                                    <% }); %>
                                <% } else { %>
                                    <p class="text-muted">No hay incidencias recientes</p>
                                <% } %>
                            </div>
                        </div>

                        <!-- Acciones Rápidas -->
                        <div class="card">
                            <div class="card-header">
                                <h6 class="m-0 font-weight-bold text-primary">
                                    <i class="fas fa-bolt"></i> Acciones Rápidas
                                </h6>
                            </div>
                            <div class="card-body">
                                <div class="d-grid gap-2">
                                    <a href="/usuarios" class="btn btn-outline-primary btn-sm mb-2">
                                        <i class="fas fa-users"></i> Gestionar Usuarios
                                    </a>
                                    <a href="/espacios" class="btn btn-outline-secondary btn-sm mb-2">
                                        <i class="fas fa-door-open"></i> Gestionar Espacios
                                    </a>
                                    <a href="/reservas" class="btn btn-outline-success btn-sm mb-2">
                                        <i class="fas fa-calendar-check"></i> Revisar Reservas
                                    </a>
                                    <a href="/incidencias" class="btn btn-outline-warning btn-sm mb-2">
                                        <i class="fas fa-exclamation-triangle"></i> Ver Incidencias
                                    </a>
                                    <a href="/configuracion" class="btn btn-outline-info btn-sm">
                                        <i class="fas fa-cog"></i> Configuración
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
